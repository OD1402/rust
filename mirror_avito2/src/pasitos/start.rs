use super::*;

// pub async fn scan_cards() -> Result<()> {
            if !ids.is_empty() {
            } else if let Some(region_facet) = region_facet {
            } else {
                Err(anyhow!("either ids or region_facet MUST be specified to scan-cards");
            }
// }


